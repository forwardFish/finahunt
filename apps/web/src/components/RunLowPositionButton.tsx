"use client";
import { startTransition, useState } from "react";
type RunResponse = { ok?: boolean; run_id?: string; latestDate?: string; message_count?: number; theme_count?: number; error?: string };
export function RunLowPositionButton({ latestRunId }: { latestRunId: string }) { const [isRunning, setIsRunning] = useState(false); const [message, setMessage] = useState(latestRunId ? `当前低位研究批次：${latestRunId}` : "暂无低位研究批次。"); async function handleRun(): Promise<void> { setIsRunning(true); setMessage("正在运行本地低位研究验收路径。"); try { const response = await fetch("/api/run-low-position", { method: "POST" }); const payload = (await response.json()) as RunResponse; if (!response.ok || payload.ok === false) throw new Error(payload.error || "run_low_position_failed"); setMessage(`运行完成：${payload.run_id || "-"}；消息 ${payload.message_count ?? 0}；题材 ${payload.theme_count ?? 0}。`); startTransition(() => { const params = new URLSearchParams(); if (payload.latestDate) params.set("date", payload.latestDate); window.location.assign(params.toString() ? `/research?${params.toString()}` : "/research"); }); } catch (error) { setMessage(`运行失败：${error instanceof Error ? error.message : "unknown_error"}`); } finally { setIsRunning(false); } } return <div className="refresh-box" data-refresh-state={isRunning ? "running" : "idle"}><button type="button" className="secondary-button" onClick={handleRun} disabled={isRunning}>{isRunning ? "运行中..." : "运行低位研究"}</button><span aria-live="polite" className="refresh-text">{message}</span></div>; }
